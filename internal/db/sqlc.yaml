version: "2"
sql:
  - engine: "mysql"
    queries: "queries/"
    schema: "schema.sql"
    gen:
      go:
        package: "dbgen"
        out: "../dbgen"
        emit_json_tags: true
        emit_pointers_for_null_types: true
